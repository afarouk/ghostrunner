<span class="team-status"><%= displayText %></span>
<div class="actions-buttons">
	<% _.each(actions, function(action) { %>
		<button type="button" data-type="interface-btn" data-action="<%= action.action %>" class="btn move"><%= action.text %></button>
	<% }); %>
</div>