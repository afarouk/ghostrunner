/*global define*/

'use strict';

define([
    '../appCache'
    ], function(appCache){
    var PlayerActionsModel = Backbone.Model.extend({
        initialize: function() {
            
        }
    });
    return PlayerActionsModel;
});
