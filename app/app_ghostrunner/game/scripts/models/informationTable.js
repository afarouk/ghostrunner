/*global define*/

'use strict';

define([
    '../appCache'
    ], function(appCache){
    var InformationTableModel = Backbone.Model.extend({
        initialize: function() {
            
        }
    });
    return InformationTableModel;
});
