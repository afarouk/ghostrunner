/*global define */

'use strict';

define([
    '../../Vent'
    ], function(Vent){
    //We need it for show modal dialogs
    var ModalsController = Mn.Object.extend({
        });

    return new ModalsController();
});
